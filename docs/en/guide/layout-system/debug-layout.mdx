# Debugging Layout Issues

When suspecting layout circular dependencies:

Use debug view:

- `debug: true` Display layout boundaries

```rust
MyView = <View> {
    debug: true,  // Display layout boundaries
}
```
